import React from 'react';
import styled from 'styled-components';
import { BaseCard } from './BaseCard';

interface SosCardProps {

}

export const SosCard = ({ }: SosCardProps) => {

	return (
		<BaseCard>

		</BaseCard>
	);
}
